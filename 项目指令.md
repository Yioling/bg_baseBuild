# 项目指令（Project Instructions）

> 本文件是 `TSForce_MentorAI`（薪火·师傅带徒 AI 导师系统）的**常驻指令**。
> 用法：在 CodeBuddy 中开始任何新会话时，先让它读取本文件（或将其配置为 CodeBuddy 的项目规则）。
> 之后你只需描述"这次要做什么"，无需再重复下面的通用约束——这正是省 token 的关键。

---

## 0. 项目一句话定位
Windows 上双击/命令即可运行的 Web 应用：师傅投喂资料→AI 精炼知识库→徒弟摸底定级→日历学习计划→当日讲义 PDF→当日复习批改→学情看板/同门战况。

## 1. 技术栈（不要偏离）
- 后端：Python + FastAPI（REST + 页面托管），SQLite（零部署）
- 前端：原生 HTML/CSS/JS 单页应用（**零构建**，由 FastAPI 直接托管）
- LLM：OpenAI 兼容协议（`openai` SDK，自定义 `base_url`），默认 DeepSeek，可切通义/Ollama；无 Key 时进"演示模式"兜底
- 嵌入：本地中文 `fastembed` + `jinaai/jina-embeddings-v2-small-zh`
- 向量检索：余弦相似度 + pickle 自研轻量库
- PDF：`reportlab` / `fpdf2` / HTML→PDF

## 2. 强制技能 / 资源引用（按内容类型触发，不要每次重写）
- **写 HTML / 前端页面 / PDF 的 HTML 模板** → 先调用 `huashu-design` 技能（高保真、响应式、适度动效，**禁止丑陋默认样式**）。
- **写 Python / 后端 / 智能体** → 参考 `awesome-python`，优先成熟库（FastAPI/SQLAlchemy/pydantic/trafilatura/openai），规范结构、异步、类型标注。
- **出 PDF / Markdown 文档** → 参考 `awesome-design-md`，美观排版而非纯文本堆砌。

## 3. 通用约束（每次都生效，不用你再念）
- 中文优先：提示词、界面文案、生成内容均为中文。
- 改动最小化：用 targeted edit（精准替换）而非整体重写；不重写无关文件。
- 必须可运行：`pip install -r requirements.txt && python run.py` 启动 `http://localhost:8000` 不报错，且不影响现有 30 个 API 路由。
- 代码提交前自检：import 完整、无语法错误、JSON 解析鲁棒（去 ```json 围栏、截取首个 `{`/`[` 到匹配括号）、LLM 调用用 `asyncio.to_thread` 包裹。
- 不要主动删减核心功能；歧义时以"贴合比赛评分 + 开箱即跑"为最高优先级。

## 4. 五个智能体（复用，勿重建）
Refiner（精炼维度+考点树）/ Assessor（分层出题批改定级）/ Planner（日历计划）/ Tutor（RAG 陪练+讲义）/ Reviewer（当日复习出题批改）。详见 `backend/agents/`。

## 5. 本次任务（每轮只填这一段）
> 在这里用 1–3 句话描述你这次具体要做什么，并给出"验收标准"。例如：
> "在 `planner.py` 中新增『锁定某天』的 `PUT /api/master/plan/{apprentice_id}/lock` 端点，复用现有 db 连接；验收：新增 pytest 用例覆盖锁定后日计划不可改。"

---

*说明：把原本散落在聊天里的重复指令固化成本文件后，每次会话可少粘贴约 200–400 行约束文本，同时保证输出风格一致。*
